using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class SpringIndicator : MonoBehaviour {

	//private Text text;

	// Use this for initialization
	void Start () {
	//	text = GetComponent<Text> ();
	}
	
	// Update is called once per frame
	void Update () {
		//text.text = ("Spring Frequency (O/P):  " + SpringControl.springFreq + " \n" + "Spring Damping Ratio (L/K):" + SpringControl.springDamp);
	}
}
